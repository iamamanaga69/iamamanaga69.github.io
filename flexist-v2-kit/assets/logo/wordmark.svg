<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 260 64" role="img" aria-label="Flexist">
  <title>Flexist</title>
  <!-- STATIC FALLBACK ONLY (email signature, social avatars, OG cards).
       On the site, render the wordmark as live HTML text next to mark.svg so it
       stays crisp, selectable, and theme-aware. Do not ship this as the header
       logo image. Convert the <text> to outlines before using it anywhere the
       font is not guaranteed. -->
  <g fill="currentColor">
    <path d="M8 12 H46 L40 20 H16 V30 H38 L32 38 H16 V56 H8 Z"/>
    <text x="62" y="45" font-family="'Bricolage Grotesque','Clash Display',system-ui,sans-serif"
          font-size="40" font-weight="600" letter-spacing="-1.6">Flexist</text>
  </g>
</svg>
